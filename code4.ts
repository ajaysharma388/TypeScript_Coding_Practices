let a: number;
let b: string;
let c: boolean;
let d: any;
let e: number[] = [1, 2, 3, 4, 5];
let f: any[] = [1, true, "hello", "a", e];

enum Color {
  Red = 0,
  Green = 1,
  Blue = 2
}

let backGroundColor = Color.Red;
// Since, TypeScript is statically typed language so,
// we have so many data types available in typescript.
